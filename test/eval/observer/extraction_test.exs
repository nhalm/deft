defmodule Deft.Eval.Observer.ExtractionTest do
  use ExUnit.Case, async: true

  @moduletag :eval

  # TODO: Implement Observer extraction evals per evals spec §1.2
  # Verify Observer extracts observations from user messages correctly
end
