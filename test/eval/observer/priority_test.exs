defmodule Deft.Eval.Observer.PriorityTest do
  use ExUnit.Case, async: true

  @moduletag :eval

  # TODO: Implement Observer priority evals per evals spec §1.2
  # Verify Observer assigns correct priorities to observations
end
