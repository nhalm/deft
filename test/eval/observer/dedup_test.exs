defmodule Deft.Eval.Observer.DedupTest do
  use ExUnit.Case, async: true

  @moduletag :eval

  # TODO: Implement Observer deduplication evals per evals spec §1.2
  # Verify Observer deduplicates redundant observations
end
