{
  "id": "lead-task-planning-002",
  "spec_version": "0.2",
  "description": "API endpoint with validation and tests",
  "tags": ["lead", "task_planning"],
  "deliverable": "Add POST /api/posts endpoint with title and body validation",
  "research_findings": {
    "contracts": [
      {
        "key": "router",
        "value": "Router is at lib/myapp_web/router.ex"
      },
      {
        "key": "post_schema",
        "value": "Post schema exists with :title, :body, :author_id fields"
      }
    ],
    "research": [
      {
        "key": "validation",
        "value": "Project uses Ecto changesets for validation"
      }
    ]
  },
  "expected_properties": {
    "min_tasks": 4,
    "max_tasks": 8,
    "has_dependencies": true,
    "has_done_states": true
  }
}
