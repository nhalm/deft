{
  "id": "lead-task-planning-003",
  "spec_version": "0.2",
  "description": "Database migration with data transformation",
  "tags": ["lead", "task_planning"],
  "deliverable": "Add status field to orders table and migrate existing orders to 'pending' status",
  "research_findings": {
    "contracts": [
      {
        "key": "order_schema",
        "value": "Order schema at lib/myapp/orders/order.ex has :id, :total, :user_id"
      }
    ],
    "research": [
      {
        "key": "migration_pattern",
        "value": "Migrations go in priv/repo/migrations/"
      },
      {
        "key": "existing_orders",
        "value": "Database has 50+ existing orders that need migration"
      }
    ]
  },
  "expected_properties": {
    "min_tasks": 4,
    "max_tasks": 8,
    "has_dependencies": true,
    "has_done_states": true
  }
}
