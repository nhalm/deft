{
  "id": "lead-steering-004",
  "spec_version": "0.3",
  "description": "Runner missed required field in migration",
  "tags": ["lead", "steering"],
  "task_description": "Create migration for users table with email, password_hash, and confirmed_at fields",
  "runner_output": "defmodule MyApp.Repo.Migrations.CreateUsers do\n  use Ecto.Migration\n\n  def change do\n    create table(:users) do\n      add :email, :string, null: false\n      add :password_hash, :string, null: false\n      timestamps()\n    end\n\n    create unique_index(:users, [:email])\n  end\nend",
  "has_error": true,
  "required_error_mentions": [
    "missing",
    "confirmed_at",
    "field"
  ],
  "required_correction_mentions": [
    "confirmed_at",
    "add",
    "timestamp"
  ]
}
