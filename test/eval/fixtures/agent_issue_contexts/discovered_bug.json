{
  "id": "agent-issue-001",
  "spec_version": "0.2",
  "description": "Agent discovers a bug while implementing a feature",
  "tags": ["agent", "bug", "discovered"],
  "context": {
    "working_on": "Add user profile update endpoint",
    "code_location": "lib/app/accounts/user.ex",
    "discovery": "While implementing the update endpoint, noticed that the email validation regex allows invalid email formats like 'user@domain' without TLD. This would allow users to register with malformed emails that can't receive verification messages.",
    "impact": "Existing users may have invalid emails in the database, blocking password reset functionality."
  },
  "expected_issue": {
    "title_contains": ["email", "validation"],
    "context_min_length": 80,
    "has_acceptance_criteria": true,
    "has_constraints": false,
    "source": "agent",
    "priority": 3,
    "is_actionable": true,
    "not_trivial": true
  }
}
