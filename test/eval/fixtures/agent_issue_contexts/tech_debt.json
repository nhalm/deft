{
  "id": "agent-issue-005",
  "spec_version": "0.2",
  "description": "Agent discovers technical debt worth tracking",
  "tags": ["agent", "tech-debt", "discovered"],
  "context": {
    "working_on": "Add rate limiting to API endpoints",
    "code_location": "lib/app_web/plugs/rate_limiter.ex",
    "discovery": "The current rate limiting implementation stores counters in ETS without any cleanup mechanism. For long-running applications, this will cause unbounded memory growth as IP addresses accumulate. A similar issue was fixed in the session store last quarter.",
    "impact": "Memory leak in production that will require periodic restarts."
  },
  "expected_issue": {
    "title_contains": ["rate", "limit", "memory", "leak"],
    "context_min_length": 80,
    "has_acceptance_criteria": true,
    "has_constraints": false,
    "source": "agent",
    "priority": 2,
    "is_actionable": true,
    "not_trivial": true
  }
}
